--- a/examples/lvgl9/lvgl9.ino
+++ b/examples/lvgl9/lvgl9.ino
@@ -59,7 +59,15 @@
     size_t len = lv_area_get_size(area);
     lv_draw_sw_rgb565_swap(color_p, len);
     esp_lcd_panel_draw_bitmap(panel_handle, area->x1, area->y1, area->x2 + 1, area->y2 + 1, (uint16_t *)color_p);
-    lv_display_flush_ready(disp);
+    // Completion is signaled by the SPI transaction callback below.
+}
+
+// esp_lcd invokes this only after DMA no longer reads LVGL's source buffer.
+static bool disp_flush_done(esp_lcd_panel_io_handle_t io,
+                            esp_lcd_panel_io_event_data_t *event, void *ctx)
+{
+    lv_display_flush_ready((lv_display_t *)ctx);
+    return false;
 }
 
 static uint32_t lv_tick_get_callback(void)
@@ -114,6 +122,10 @@
     lv_display_set_buffers(disp_drv, buf, NULL, lv_buffer_size, LV_DISPLAY_RENDER_MODE_PARTIAL);
     lv_display_set_color_format(disp_drv, LV_COLOR_FORMAT_RGB565);
     lv_display_set_flush_cb(disp_drv, disp_flush);
+    const esp_lcd_panel_io_callbacks_t callbacks = {
+        .on_color_trans_done = disp_flush_done,
+    };
+    ESP_ERROR_CHECK(esp_lcd_panel_io_register_event_callbacks(io_handle, &callbacks, disp_drv));
     lv_tick_set_cb(lv_tick_get_callback);
 
     lv_obj_t *label = lv_label_create(lv_screen_active());
